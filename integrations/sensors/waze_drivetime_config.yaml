###
# Waze Travel Time Config - DISABLED
# 
# ⚠️ NOTE: The Waze Travel Time integration should now be configured via UI:
# Settings > Devices & Services > Add Integration > Waze Travel Time
###

# LEGACY CONFIG - Use UI instead
# - platform: waze_travel_time
#   name: Home to Destination
#   origin: zone.home
#   destination: sensor.waze_dest_address
#   region: 'US'

# Empty list placeholder to prevent YAML errors with !include_dir_merge_list
[]
