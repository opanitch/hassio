###
# Philips Hue Config - DISABLED
# 
# ⚠️ NOTE: The Hue integration should now be configured via UI:
# Settings > Devices & Services > Add Integration > Philips Hue
#
# The UI-based integration provides:
# - Automatic bridge discovery
# - Better entity management
# - Support for Hue scenes and rooms
###

# LEGACY CONFIG - Use UI instead
# bridges:
#   - host: !secret hue_bridge_host
#     allow_unreachable: true
#     allow_hue_groups: true

# Empty list placeholder to prevent YAML errors with !include_dir_merge_list
[]
